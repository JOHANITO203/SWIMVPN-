# Android
android/.gradle/
android/.gradle-user-home/
android/build/
android/local.properties

# Windows desktop port — local build caches (never commit)
windows/.gradle/
windows/build/
windows/.kotlin/
android/Generated/
android/app/build/
android/app/.cxx/
android/app/java_pid*.hprof
*.hprof

# IDE
.idea/
.idea/caches/
.idea/modules.xml
.idea/workspace.xml
android/.idea/
android/.idea/caches/
android/.idea/workspace.xml
*.iml

# Gradle Distribution (ne pas versionner le dossier dists téléchargé)
android/wrapper/dists/

# Node / JS
node_modules/
build/
dist/
dist-ssr/
coverage/

# Signing / secrets (NEVER commit keystores or signing credentials)
*.jks
*.keystore
keystore.properties
**/keystore.properties
swimsignature

# Misc
.DS_Store
*.log
.env*
!.env.example

# Design mocks — local-only working references (not deployed; the live UI is in src/)
docs/design/
